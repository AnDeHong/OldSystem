<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.mapper.UserInfoMapper">
    <!-- 查询用户信息 -->
    <select id="getUsers" resultType="com.example.entity.UserInfo">
        SELECT * FROM userinfo
        <where>
            <if test="id != null and id != '' ">
                and id = #{id}
            </if>
            <if test="username != null and username != '' ">
                and username LIKE "%"#{username}"%"
            </if>
            <if test="phone != '' and phone != null ">
                and phone = #{phone}
            </if>
          and  delete_mark = 'N'
        </where>
    </select>
    <select id="getFamily" resultType="com.example.entity.Family">
        select * from family where user_id = #{userId}
    </select>
</mapper>